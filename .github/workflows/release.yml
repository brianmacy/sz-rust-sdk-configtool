name: Release

on:
  push:
    tags:
      - 'v*'

env:
  CARGO_TERM_COLOR: always

# The release step creates the GitHub Release and attaches the built libs. That needs write access;
# the default GITHUB_TOKEN is read-only, so without this every release 403'd ("Resource not accessible
# by integration") at the upload step — it had failed on every tag since v0.1.0.
permissions:
  contents: write

jobs:
  build-and-release:
    name: Build and Release
    runs-on: ${{ matrix.os }}
    strategy:
      # Don't cancel the other platform builds if one leg hiccups (e.g. a transient release-create race).
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            artifact_name: libsz_configtool_lib.so
            artifact_path: target/release/libsz_configtool_lib.so
          - os: macos-latest
            target: x86_64-apple-darwin
            artifact_name: libsz_configtool_lib.dylib
            artifact_path: target/release/libsz_configtool_lib.dylib
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            artifact_name: sz_configtool_lib.dll
            artifact_path: target/release/sz_configtool_lib.dll
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
        with:
          toolchain: stable
          targets: ${{ matrix.target }}

      - name: Build release
        run: cargo build --lib --release --target ${{ matrix.target }}

      - name: Create tarball (Unix)
        if: runner.os != 'Windows'
        run: |
          cd target/${{ matrix.target }}/release
          tar czf ${{ matrix.artifact_name }}.tar.gz ${{ matrix.artifact_name }}
          mv ${{ matrix.artifact_name }}.tar.gz ../../..

      - name: Create zip (Windows)
        if: runner.os == 'Windows'
        run: |
          cd target/${{ matrix.target }}/release
          7z a ${{ matrix.artifact_name }}.zip ${{ matrix.artifact_name }}
          move ${{ matrix.artifact_name }}.zip ..\..\..

      - name: Upload Release Asset (Unix)
        if: runner.os != 'Windows'
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          files: ${{ matrix.artifact_name }}.tar.gz
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Asset (Windows)
        if: runner.os == 'Windows'
        uses: softprops/action-gh-release@718ea10b132b3b2eba29c1007bb80653f286566b # v3.0.1
        with:
          files: ${{ matrix.artifact_name }}.zip
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Future: Publish to crates.io
  # publish-crates-io:
  #   name: Publish to crates.io
  #   needs: build-and-release
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v6
  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # stable
  #     - name: Publish
  #       run: cargo publish --token ${{ secrets.CRATES_IO_TOKEN }}
